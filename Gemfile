source "http://rubygems.org"

# Will automatically pull in this gem and all its
# dependencies specified in the gemspec
gem "calculated", :path => File.expand_path("..", __FILE__)
gem "yard"

# These are development dependencies 
group :test do
  gem "rake"
  gem "rspec", "2.4.0"
  gem "artifice", "0.5"
  gem "sinatra", "1.0"
  gem "rack-contrib", "0.9.2", :require => "rack/contrib"
  gem "rack", "1.1.0"
end
